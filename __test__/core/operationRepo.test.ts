describe('OperationRepo tests', () => {
  test('OperationRepo executor store has executor for each model name', async () => {});
  test('Model repo delta broadcast is received and processed by operation repo', async () => {});
  test('Add Subscriptions: Multiple delta broadcasts -> two operations of change type: add', async () => {});
  test('Update User Properties-> one operation of change type: update', async () => {});
  test('Update Identity: Multiple delta broadcasts -> one operation of change type: update', async () => {});
});