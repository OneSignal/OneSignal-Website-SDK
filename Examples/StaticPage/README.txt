You must host these static files on:
1. A localhost http connection.
2. Or a https connection with a valid recognized certificate authority(Chrome does not allow self signed certs with push notifications).

You can not use a file:/// URL from your browser to test push notifications.