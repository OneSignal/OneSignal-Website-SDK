import { UserStateSynchronizer } from "./UserStateSynchronizer";

class UserStateSynchronizerForSW extends UserStateSynchronizer {
  // @override
  public onSession(): void {
    // TODO: Move code from SW into here, for query pages and waiting.
  }
}
